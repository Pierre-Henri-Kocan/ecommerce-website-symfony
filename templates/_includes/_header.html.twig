<nav class="navbar navbar-expand-lg bg-primary navbar-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Kommercial</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link {{"app_main" in app.request.attributes.get('_route') ? " active":" "}}" aria-current="page" href="{{ path('app_main') }}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Features</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Pricing</a>
				</li>
			</ul>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
			{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="#">Mon compte</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Me déconnecter</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link {{"app_login" in app.request.attributes.get('_route') ? " active":" "}}" aria-current="page" href="{{ path('app_login') }}">Me connecter</a>
				</li>
				<li class="nav-item">
					<a class="nav-link {{"app_register" in app.request.attributes.get('_route') ? " active":" "}}" aria-current="page" href="{{ path('app_register') }}">M'inscrire</a>
				</li>
			{% endif %}
			</ul>
		</div>
	</div>
</nav>
